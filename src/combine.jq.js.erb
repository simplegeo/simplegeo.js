(function(window, undefined){
  (function($){
    //= require <core>
    //= require <client>
    <% includes.each do |include| %>
      //= require <<%= include %>>
    <% end %>
    window.simplegeo = simplegeo;
  })(jQuery);
})(window);
