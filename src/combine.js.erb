(function(window, undefined){
  //= require <jqmin>

  (function($){
    //= require <core>
    //= require <client>
    <% includes.each do |include| %>
      //= require <<%= include %>>
    <% end %>
    window.simplegeo = simplegeo;
  })(jQmin);
})(window);
