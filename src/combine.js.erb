(function(window, undefined){
//= require <jqmin>

if (window.simplegeo === undefined) {
  window.simplegeo = {};
}

(function(simplegeo,$){
//= require <core>
<% includes.each do |include| %>
//= require <<%= include %>>
<% end %>
})(window.simplegeo, jQuery);
})(window);
